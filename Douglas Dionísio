echo "# tabuada-simplificada" >> README.md 
git init 
git add README.md 
git commit -m "first commit" 
git branch -M main 
git remote add origin https://github.com/ddionisio1/tabuada-simplificada. git
 git push -u origem principal
 
 # tabuada utilizando dois comandos if, else e while

tabuada = int(input('Digite um número do [1 ao 10] para saber a tabuada completa: '))
multiplicador = 0
resultado = 0

print(f'A tabuada do {tabuada} é:')

if tabuada >= 1 and tabuada <= 10:
    while multiplicador <= 10:
        print(f'({tabuada} x {multiplicador}) = {resultado}')
        multiplicador += 1
        resultado = multiplicador * tabuada
        
else:
    print('Inválido, digite novamente!')
